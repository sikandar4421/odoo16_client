<odoo>
    <data>
        <!-- explicit list view definition -->
        <record id="custom_sale_order_inherit_new" model="ir.ui.view">
            <field name="name">custom.sale.order.inherit.new</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="after">
                    <field name="project_id"/>
                </field>

            </field>
        </record>

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="sale_order_report.action_window">
              <field name="name">Sale_order_report window</field>
              <field name="res_model">sale_order_report.sale_order_report</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="sale_order_report.action_server">
              <field name="name">Sale_order_report server</field>
              <field name="model_id" ref="model_sale_order_report_sale_order_report"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="Sale_order_report" id="sale_order_report.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="sale_order_report.menu_1" parent="sale_order_report.menu_root"/>
            <menuitem name="Menu 2" id="sale_order_report.menu_2" parent="sale_order_report.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="sale_order_report.menu_1_list" parent="sale_order_report.menu_1"
                      action="sale_order_report.action_window"/>
            <menuitem name="Server to list" id="sale_order_report" parent="sale_order_report.menu_2"
                      action="sale_order_report.action_server"/>
        -->
    </data>
</odoo>